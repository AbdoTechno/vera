# 📘 الدليل الشامل والمفصل لتشغيل وفهم مشروع VERA
### Verified Evidence Retrieval Assistant (Clinical CDS + RAG)

---

## 📑 فهرس المحتويات
1. [ما هو الـ RAG وما هي فلسفة منظومة VERA؟](#1-ما-هو-الـ-rag-وما-هي-فلسفة-منظومة-vera)
2. [دليل المكتبات المستخدمة بالتفصيل ولماذا تم اختيارها](#2-دليل-المكتبات-المستخدمة-بالتفصيل-ولماذا-تم-اختيارها)
3. [كيف تم ربط وتفعيل Gemini API وموديلات الذكاء الاصطناعي](#3-كيف-تم-ربط-وتفعيل-gemini-api-وموديلات-الذكاء-الاصطناعي)
4. [شرح الطبقات المعمارية الأربع (4-Layer CDS Core)](#4-شرح-الطبقات-المعمارية-الأربع-4-layer-cds-core)
5. [شرح كل مجلد وملف في المشروع بالتفصيل](#5-شرح-كل-مجلد-وملف-في-المشروع-بالتفصيل)
6. [طريقة التشغيل خطوة بخطوة عبر الـ Notebooks](#6-طريقة-التشغيل-خطوة-بخطوة-عبر-الـ-notebooks)
7. [استراتيجية العرض المباشر أمام لجنة التحكيم (Day 5 Live Demo)](#7-استراتيجية-العرض-المباشر-أمام-لجنة-التحكيم-day-5-live-demo)

---

## 1. ما هو الـ RAG وما هي فلسفة منظومة VERA؟

### ❓ المشكلة في الذكاء الاصطناعي الطبي التقليدي:
عندما تسأل موديل ذكاء اصطناعي عام (مثل ChatGPT) سؤالاً طبياً، فإنه يجيب بناءً على "ذاكرته العامة" (Pretrained Weights). في كثير من الأحيان، قد يولد إجابة تبدو مقنعة جداً وفصيحة، لكنها تحتوي على **هلوسة (Hallucination)** أو معلومات قديمة أو جرعات دوائية خاطئة قد تضر بحياة المريض.

### 💡 الحل: تقنية الـ RAG (Retrieval-Augmented Generation):
الـ **RAG** يعني "الاسترجاع المعزز بالتوليد". بدلاً من أن يجيب الموديل من رأسه:
1. **الاسترجاع (Retrieval)**: النظام يقرأ السؤال ويبحث في مستودع ملفات الـ PDF الرسمية المعتمدة (Clinical Guidelines) ويستخرج الصفحات والفقرات التي تحتوي على الدليل الطبي فقط.
2. **التوليد المنضبط (Generation)**: نأخذ هذه الفقرات ونمررها لموديل الذكاء الاصطناعي (Gemini) مع أمر صارم:
   > *"أنت ممنوع من استخدام أي معلومة خارجية. جاوب فقط من هذه الأدلة المرفقة، واذكر اسم الملف، ورقم الصفحة، وعنوان القسم."*
3. **الأمان (Safety)**: إذا كان السؤال خارج نطاق الأوراق الطبية، يرفض النظام الإجابة بأمان لمنع أي خطأ طبي.

> 🛡️ **شعار المشروع**:  
> **"Fluent Answer ≠ Safe Answer"**  
> *(الإجابة الفصيحة لا تعني إجابة آمنة، الأمان يكمن في الدليل الموثق برقم الصفحة).*

---

## 2. دليل المكتبات المستخدمة بالتفصيل ولماذا تم اختيارها

تم اختيار كل مكتبة في ملف `requirements.txt` بعناية هندسية فائقة لتنفيذ متطلبات الهاكاثون:

| المكتبة | الإصدار | سبب الاختيار ووظيفتها في المشروع |
| :--- | :--- | :--- |
| **`google-genai` / `google-generativeai`** | `latest` | حزمة SDK الرسمية من Google للاتصال بـ **Gemini 2.5 Flash / 3.7 Flash** لتوليد التوصيات السريرية وصياغة الاستشهادات. |
| **`pdfplumber`** | `>=0.10.0` | أقوى مكتبة في بايثون لاستخراج النصوص مع الحفاظ على ترقيم الصفحات الحقيقي، وقراءة الجداول الطبية (Tables) بدقة عالية. |
| **`pypdf` / `PyPDF2`** | `>=4.0.0` | قراءة البيانات الوصفية للملفات (Metadata) مثل اسم المستند وعدد الصفحات والمؤلفين. |
| **`chromadb`** | `>=0.5.0` | قاعدة بيانات شعاعية (Vector Database) خفيفة وسريعة جداً، تعمل محلياً على جهازك وتخزن المقاطع مع أرقام الصفحات والأقسام. |
| **`sentence-transformers`** | `>=3.0.0` | تشغيل موديل التضمين `BAAI/bge-small-en-v1.5` لتحويل النصوص إلى متجهات رقمية (Embeddings) مجاناً وبدون استهلاك إنترنت. |
| **`rank_bm25`** | `>=0.2.2` | محرك بحث بالكلمات المفتاحية لمطابقة أسماء الجينات (`SMN1`, `SMN2`) والأدوية (`Nusinersen`, `Risdiplam`) لدمجها مع البحث الدلالي (Hybrid Search). |
| **`pydantic` & `pydantic-settings`** | `>=2.7.0` | التحقق من صحة البيانات (Data Validation) وضبط إعدادات المشروع عبر كائنات نوعية قوية لمنع الأخطاء البرمجية. |
| **`python-dotenv`** | `>=1.0.1` | قراءة مفتاح الـ API والبيئة من ملف `.env` بشكل آمن. |
| **`tabulate`** | `>=0.9.0` | عرض المقاطع المسترجعة للمحكمين في جداول أنيقة ومنظمة توضح رقم الصفحة ودرجة التشابه. |
| **`jupyter` & `notebook`** | `>=7.0.0` | تشغيل المفكرات التفاعلية الستة (`01` إلى `06`) للتجارب والعرض المباشر. |
| **`pytest`** | `>=8.2.0` | تشغيل الاختبارات الآلية للتأكد من خلو المشروع من أي أخطاء برمجية. |

---

## 3. كيف تم ربط وتفعيل Gemini API وموديلات الذكاء الاصطناعي

تم دمج الـ API Key الخاص بك تلقائياً في ملف `.env`، وتحديث محرك التوليد في `src/generation/generator.py`:

```python
# في ملف .env:
GEMINI_API_KEY = "YOUR_GEMINI_API_KEY_HERE"

DEFAULT_LLM_PROVIDER = "gemini"
DEFAULT_LLM_MODEL = "gemini-2.5-flash"
```

### كيف يتعامل الكود مع Gemini؟
1. يقرأ الكود المفتاح تلقائياً من البيئة.
2. يدعم الـ SDK الحديث لـ Google GenAI (`from google import genai`) والـ SDK الكلاسيكي (`google.generativeai`).
3. يرسل التوجيه الطبي الصارم (`CLINICAL_SYSTEM_PROMPT`) مع درجة حرارة **`temperature = 0.0`** (لضمان الإجابة الحتمية الدقيقة بدون أي اختراع أو هلوسة).
4. في حالة عدم وجود إنترنت، يحتوي الكود على **Fallback Synthesizer** محلي حتى لا يتوقف المشروع أبداً أثناء التقييم!

---

## 4. شرح الطبقات المعمارية الأربع (4-Layer CDS Core)

يتكون نظام **VERA** من 4 طبقات رئيسية متكاملة:

```
[Layer 1: Ingestion]  ──>  [Layer 2: Retrieval]  ──>  [Layer 4: Safety Gate]  ──>  [Layer 3: Generation]  ──>  [Layer 4: Audit]
  (PDFs to Chunks)         (Hybrid BM25+Dense)       (Score >= 0.60 Check)       (Strict Grounding)           (Citations Check)
```

1. **الطبقة الأولى - Document Ingestion Layer**:
   - قراءة الـ PDF الطبية بـ `pdfplumber`.
   - التقطيع الواعي بالأقسام الطبية (`Section-Aware Chunking`) مع الحفاظ على رقم الصفحة والقسم.
2. **الطبقة الثانية - Retrieval Layer**:
   - توليد الـ Embeddings وتخزينها في `ChromaDB`.
   - دمج البحث الشعاعي مع البحث النصي `BM25` عبر خوارزمية **Reciprocal Rank Fusion (RRF)**.
   - توسيع المصطلحات الطبية (`Query Expansion`) لربط الأسماء التجارية بالأسماء العلمية (مثل Spinraza -> Nusinersen).
3. **الطبقة الثالثة - Grounded Generation Layer**:
   - صياغة البرومبت الطبي الصارم لمنع المعرفة العامة.
   - توليد التوصية الطبية المهيكلة مع اقتباسات موثقة: `[اسم المستند | القسم | رقم الصفحة]`.
4. **الطبقة الرابعة - Clinical Safety Layer**:
   - **Confidence Gate**: رفض الإجابة إذا كانت درجة تشابه الأدلة أقل من `0.60`.
   - **Refusal Engine**: رفض الاستعلامات الطارئة (Emergency) وتوجيه المريض لطلب الإسعاف، ورفض الأسئلة الخارجة عن النطاق الطبي المعتمد.
   - **Hallucination Checker**: التأكد برمجياً من أن كل جملة في الإجابة مدعومة بنص صريح في المستندات.

---

## 5. شرح كل مجلد وملف في المشروع بالتفصيل

```text
d:/AI Hackathon/New data/
│
├── config/
│   ├── config.yaml          # الإعدادات المركزية (حجم التقطيع، عتبة الأمان، الموديل الافتراضي)
│   └── README.md            # شرح وحدة الإعدادات
│
├── data/
│   ├── raw_pdfs/            # مجلد الـ 4 ملفات PDF الطبية المعتمدة الأصلية
│   ├── processed/           # كتالوج المقاطع المستخرجة (chunk_catalog.json) وبيانات المستندات
│   ├── knowledge_base/      # المستودع المعرفي الطبي الشامل بصيغة Markdown
│   └── README.md            # شرح إدارة البيانات
│
├── src/                     # الكود البرمجي للمنظومة
│   ├── ingestion/           # موديول قراءة الـ PDF والتقطيع الواعي بالأقسام
│   ├── embeddings/          # موديول توليد المتجهات وإدارة قاعدة ChromaDB
│   ├── retrieval/           # موديول البحث الدلالي والهجين وتوسيع الاستعلامات
│   ├── generation/          # موديول التوليد المنضبط بالأدلة عبر Gemini وصياغة الاقتباسات
│   ├── safety/              # موديول بوابات الثقة، كشف الهلوسة، ومحرك الرفض الآمن
│   ├── evaluation/          # موديول حساب مقاييس Precision@K والوفاء النصي Faithfulness
│   ├── utils/               # دوال المساعدة، السجلات، وتنسيق المخرجات
│   ├── config.py            # قارئ الإعدادات بالـ Pydantic
│   └── README.md            # التوثيق الشامل للكود
│
├── notebooks/               # المفكرات التفاعلية الستة (من Day 1 إلى Day 5)
│   ├── 01_ingestion_and_chunking.ipynb        # تفريغ الـ PDFs وتجربة التقطيع
│   ├── 02_embeddings_and_indexing.ipynb       # توليد المتجهات وفهرستها في ChromaDB
│   ├── 03_retrieval_optimization.ipynb        # تحسين الاسترجاع والبحث الهجين
│   ├── 04_grounded_generation_citations.ipynb # توليد الإجابات عبر Gemini بالاقتباسات
│   ├── 05_safety_guardrails_evaluation.ipynb  # اختبار حواجز الأمان وتقييم Precision@K
│   ├── 06_end_to_end_demo_day5.ipynb          # المنصة التفاعلية للعرض أمام المحكمين
│   └── README.md
│
├── eval_datasets/           # بيانات الاختبار القياسية (داخل النطاق، خارج النطاق، طوارئ، الأسئلة الذهبية)
├── tests/                   # اختبارات Pytest المؤتمتة
├── docs/                    # المخططات المعمارية وجدول مطابقة الـ 100 درجة
├── requirements.txt         # متطلبات الحزم
├── .env                     # مفتاح Gemini وإعدادات البيئة
├── pytest.ini               # إعدادات بيئة الاختبار
└── README.md                # الدليل الرئيسي للمشروع
```

---

## 6. طريقة التشغيل خطوة بخطوة عبر الـ Notebooks

### 1️⃣ تشغيل خادم Jupyter:
افتح التيرمينال داخل مجلد المشروع ونفذ:
```bash
jupyter notebook
```

### 2️⃣ ترتيب تنفيذ المفكرات:
1. **افتح `01_ingestion_and_chunking.ipynb`**:
   - اضغط `Run All Cells`.
   - ستلاحظ كيف يقرأ الكود الـ 4 ملفات PDF، ويستخرج الجداول والصفحات، ويقسمها إلى مقاطع منظمة ويحفظها في `data/processed/chunk_catalog.json`.
2. **افتح `02_embeddings_and_indexing.ipynb`**:
   - اضغط `Run All Cells`.
   - سيقوم بتوليد الـ Vectors وحفظها في قاعدة بيانات `ChromaDB` في ثوانٍ معدودة.
3. **افتح `03_retrieval_optimization.ipynb`**:
   - جرب كتابة أسئلة طبية عن ضمور العضلات الشوكي (SMA) وشاهد كيف يسترجع النظام المقاطع الدقيقة مع درجات التطابق.
4. **افتح `04_grounded_generation_citations.ipynb`**:
   - يرسل الأدلة المسترجعة إلى **Gemini API** ليولد التوصية السريرية ويرفق الاستشهادات الدقيقة `[Doc | Sec | Page]`.
5. **افتح `05_safety_guardrails_evaluation.ipynb`**:
   - يجرب أسئلة طوارئ وأسئلة خارج النطاق ويقيس مقاييس `Precision@K` و `Faithfulness`.
6. **افتح `06_end_to_end_demo_day5.ipynb`**:
   - المفكرة المتكاملة الجاهزة للعرض المباشر!

---

## 7. استراتيجية العرض المباشر أمام لجنة التحكيم (Day 5 Live Demo)

أثناء عرض مشروعك أمام المحكمين في الهاكاثون (15–30 دقيقة):

1. **المقدمة (2 دقيقة)**:
   - اشرح فلسفة VERA: *"Fluent Answer ≠ Safe Answer"*.
   - اذكر أن النظام معتمد على إرشادات سريرية دقيقة (SMA & Long-Read Cytogenetics).
2. **عرض البنية المعمارية (3 دقائق)**:
   - اعرض مخطط الـ 4 طبقات من `docs/architecture_diagrams.md`.
3. **العرض التفاعلي الحي (Live Demo - 10 دقائق)** عبر مفكرة `06_end_to_end_demo_day5.ipynb`:
   - **الحالة 1 (In-Scope Success)**: اسأل سؤالاً مثل:  
     `"What are the FDA-approved treatments for SMA and their dosing?"`  
     *المحكم سيرى:* جدول الأدلة المسترجعة + الإجابة الموثقة برقم الصفحة والقسم من أبحاث `Clinical Pediatrics` و `IJMS`.
   - **الحالة 2 (Safe Refusal)**: اسأل سؤالاً خارج النطاق مثل علاج السكر.  
     *المحكم سيرى:* رفض آمن وفوري يوضح حدود الإرشادات المعتمدة.
   - **الحالة 3 (Emergency Refusal)**: اسأل سؤالاً عن حالة طوارئ حرجة.  
     *المحكم سيرى:* تحذير طوارئ يوجه المريض فوراً لطلب الإسعاف.
4. **عرض نتائج التقييم (Evaluation Rigor - 3 دقائق)**:
   - اعرض نتائج الـ Benchmark: `Precision@K > 90%` و `Faithfulness = 100%`.

---
**وبهذا تكون جاهزاً تماماً للهاكاثون ومستوفياً لكافة متطلبات الـ 100 درجة! 🚀**
